# News app
News app made with React Native and newsdata.io API.

Simple news app that fetches data from API and displays in feed. Used modals and WebView for article information.

Optimized for Android.

## Todo.

* Enhance web support (Redirect instead of unsupported WebView, some style issues)
* Add category display

I will try to implement these features in near future.

## Note

API key should be defined in ".env" file as "API_KEY" variable.